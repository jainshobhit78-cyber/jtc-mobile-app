name: Build Native Android APK

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm install --legacy-peer-deps

      - name: Install React Native CLI
        run: npm install --save-dev @react-native-community/cli @react-native-community/cli-platform-android --legacy-peer-deps

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Expo Prebuild Android Project
        run: npx expo prebuild --platform android

      - name: Create Assets Directory
        run: mkdir -p android/app/src/main/assets

      - name: Bundle JavaScript into APK
        run: |
          npx react-native bundle \
            --platform android \
            --dev false \
            --entry-file index.js \
            --bundle-output android/app/src/main/assets/index.android.bundle \
            --assets-dest android/app/src/main/res

      - name: Remove Duplicate Resources
        run: |
          find android/app/src/main/res/drawable-* -type f -name "*.png" -delete 2>/dev/null || true
          find android/app/src/main/res/raw -type f -name "*.json" -delete 2>/dev/null || true

      - name: Build Native Debug APK with Embedded Bundle
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Create or Update GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.2-standalone
          name: JTC B2B App - Standalone APK
          body: |
            Fully standalone native Android APK for JTC Customer B2B portal.
            JS bundle is embedded inside — no dev server needed.
            Just install and run.
          draft: false
          prerelease: false
          files: |
            android/app/build/outputs/apk/debug/app-debug.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
